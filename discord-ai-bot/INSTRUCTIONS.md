# 🤖 إعداد بوت ديسكورد الذكي (Discord AI Bot)

لقد قمت بإنشاء ملفات البوت لك! ولكن هناك خطوة واحدة مهمة جداً يجب عليك القيام بها لكي يعمل البوت: **إحضار التوكن (Token)**.

الويب هوك (Webhook) الذي أرسلته يستخدم فقط لإرسال الرسائل، لكن لكي يستطيع البوت **قراءة** رسائل الناس والرد عليهم، نحتاج إلى "Bot Token".

## 🛠️ الخطوات المطلوبة منك:

### 1. إنشاء تطبيق في ديسكورد
1. اذهب إلى [Discord Developer Portal](https://discord.com/developers/applications).
2. اضغط على زر **New Application** (أعلى اليمين).
3. سمّهِ (مثلاً: `T3N AI Assistant`) واضغط **Create**.

### 2. إنشاء البوت والحصول على التوكن
1. من القائمة اليسرى، اضغط على **Bot**.
2. اضغط على زر **Reset Token** (أو Copy Token) وانسخ الرمز الطويل الذي سيظهر لك.
   *   🔴 **مهم جداً:** هذا الرمز سري! لا تشاركه مع أحد.
3. **ألصق التوكن** في ملف `index.js`:
   *   افتح الملف `index.js`.
   *   ابحث عن السطر رقم 12: `const DISCORD_BOT_TOKEN = "PASTE_YOUR_BOT_TOKEN_HERE";`.
   *   استبدل الكلام الموجود بين علامتي التنصيص بالتوكن الذي نسخته.

### 3. تفعيل الصلاحيات (Intents)
1. في نفس صفحة البوت (في الموقع)، انزل للأسفل قليلاً إلى قسم **Privileged Gateway Intents**.
2. فعّل الخيارات التالية (اجعلها زرقاء):
   *   ✅ **Presence Intent**
   *   ✅ **Server Members Intent**
   *   ✅ **Message Content Intent** (مهم جداً لقراءة الرسائل!)
3. اضغط **Save Changes** في الأسفل.

### 4. (اختياري) جعل البوت يرد تلقائياً في قناة محددة
إذا أردت أن يرد البوت على **كل الرسائل** في قناة `#ai` بدون أن تمنشنه:
1. فعل وضع المطور في ديسكورد:
   *   `User Settings` (الإعدادات) -> `Advanced` (خيارات متقدمة) -> فعّل `Developer Mode`.
2. اذهب لقناة `#ai` واضغط عليها بالزر اليمين واختر **Copy Channel ID**.
3. الصق الآيدي في ملف `index.js` في السطر رقم 14:
   ```javascript
   const AUTO_REPLY_CHANNEL_ID = "123456789012345678"; // ضع الآيدي هنا
   ```

### 5. تشغيل البوت
1. افتح موجه الأوامر (Terminal) في مجلد `discord-ai-bot`.
2. اكتب الأمر التالي لتثبيت المكتبات (إذا لم تكن مثبتة):
   ```bash
   npm install
   ```
3. شغل البوت بالأمر:
   ```bash
   node index.js
   ```

إذا ظهرت لك رسالة `✅ Bot is Ready!`، فهذا يعني أن البوت يعمل بنجاح! 🎉

---

### 📝 ملاحظة حول الويب هوك
الرابط الذي أعطيتني إياه (`...https://discord.com/api/webhooks`) قمت باستخدامه في البوت كميزة إضافية!
أي محادثة تدور بين البوت والمستخدم سيقوم البوت بإرسال نسخة منها (Log) إلى القناة الخاصة بالويب هوك، لكي تستطيع مراجعة المحادثات لاحقاً.
